Concept Check

When would you use a bar chart vs a histogram?
we use bar chart when we categorical analysis and data is also discrete like makrs of student in different subjects while histogram is also 
a data showing technique when you have continuous data and you want it to see in group wise and each category touches 
each other as well.

What is the difference between plt.plot() and sns.lineplot()?
.plot() is a matplolib function and makes a line graph and gives us the control over the graph while sns.lineplot() is a seabor function built on the top of matplotlib and makes more descriptive and clean line graph.

What does a boxplot actually show — what are the whiskers?
boxplot shows us the q1 and q3 and median and outlier as a dot on the upper side and whiskers are the lines moving outside the quartiles showing the smallest and largest non-outlier values.

How do you plot multiple charts in one figure using Matplotlib? 
using .subplot(2,2,3)-->means 2 rows,2 columns and Select the 3rd plotting position

What is a heatmap useful for and what kind of data does it work best with?
when we want to find co-relation between data and values closest to 1 or -1 shows strong co-relation.It works best with numerical data.