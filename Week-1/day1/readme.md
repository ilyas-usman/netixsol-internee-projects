Concept Check (self-graded, 5 Qs)

What’s the difference between a list and a tuple?
List is ordered collection of items and it is mutable means it can be chnages once created eother updating,deleting or adding new values as well,
while tuple is immutable collection of items and i cannot be chnaged once created it used where you know data remians same and list is used where 
you are not sure about the data and it can be chnaged over time.

Write a function that returns the square of a number.

def square(num):

    return num*num


What does *args do in a function signature?
*args will help us to use function with multiple types of inputs like it is not bound to specific variables or data types.It can accept 
any type or any number of inputs to process the funcion which is written by the programmer.

What’s the output of a basic for loop over range(5)?

0 1 2 3 4

What is a dictionary comprehension?
Dictionary comprehension is a short and efficient way to create a dictionary using a single line of code. It 
uses a for loop and can also include an if condition.

{key: value for item in iterable}
//Square of a number 
{x:x*x for x in range(1,6)}






